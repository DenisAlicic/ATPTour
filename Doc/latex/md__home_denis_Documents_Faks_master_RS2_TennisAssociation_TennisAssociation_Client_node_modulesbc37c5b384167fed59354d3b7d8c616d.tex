Given a path string, return a generator that walks up the path, emitting each dirname.

So, to get a platform-\/portable walk up, instead of doing something like this\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{for (let p = dirname(path); p;) \{}
\DoxyCodeLine{}
\DoxyCodeLine{  // ... do stuff ...}
\DoxyCodeLine{}
\DoxyCodeLine{  const pp = dirname(p)}
\DoxyCodeLine{  if (p === pp)}
\DoxyCodeLine{    p = null}
\DoxyCodeLine{  else}
\DoxyCodeLine{    p = pp}
\DoxyCodeLine{\}}

\end{DoxyCode}


Or this\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{for (let p = dirname(path); !isRoot(p); p = dirname(p)) \{}
\DoxyCodeLine{  // ... do stuff ...}
\DoxyCodeLine{\}}

\end{DoxyCode}


You can do this\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{const walkUpPath = require('walk-\/up-\/path')}
\DoxyCodeLine{for (const p of walkUpPath(path)) \{}
\DoxyCodeLine{  // ... do stuff ..}
\DoxyCodeLine{\}}

\end{DoxyCode}
\hypertarget{md__home_denis_Documents_Faks_master_RS2_TennisAssociation_TennisAssociation_Client_node_modulesbc37c5b384167fed59354d3b7d8c616d_autotoc_md13765}{}\doxysection{API}\label{md__home_denis_Documents_Faks_master_RS2_TennisAssociation_TennisAssociation_Client_node_modulesbc37c5b384167fed59354d3b7d8c616d_autotoc_md13765}

\begin{DoxyCode}{0}
\DoxyCodeLine{const walkUpPath = require('walk-\/up-\/path')}

\end{DoxyCode}


Give the fn a string, it\textquotesingle{}ll yield all the directories walking up to the root. 