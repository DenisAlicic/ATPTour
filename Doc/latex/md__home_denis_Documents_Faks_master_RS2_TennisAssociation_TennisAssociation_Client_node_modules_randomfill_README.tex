\href{https://www.npmjs.org/package/randomfill}{\texttt{ }}

randomfill from node that works in the browser. In node you just get crypto.\+random\+Bytes, but in the browser it uses .crypto/ms\+Crypto.\+get\+Random\+Values


\begin{DoxyCode}{0}
\DoxyCodeLine{var randomFill = require('randomfill');}
\DoxyCodeLine{var buf}
\DoxyCodeLine{randomFill.randomFillSync(16);//get 16 random bytes}
\DoxyCodeLine{randomFill.randomFill(16, function (err, resp) \{}
\DoxyCodeLine{  // resp is 16 random bytes}
\DoxyCodeLine{\});}

\end{DoxyCode}
 