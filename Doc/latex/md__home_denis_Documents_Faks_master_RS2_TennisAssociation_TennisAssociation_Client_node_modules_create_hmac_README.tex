\href{https://www.npmjs.org/package/create-hmac}{\texttt{ }} \href{https://travis-ci.org/crypto-browserify/createHmac}{\texttt{ }} \href{https://david-dm.org/crypto-browserify/createHmac\#info=dependencies}{\texttt{ }}

\href{https://github.com/feross/standard}{\texttt{ }}

Node style HMACs for use in the browser, with native HMAC functions in node. API is the same as HMACs in node\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{var createHmac = require('create-\/hmac')}
\DoxyCodeLine{var hmac = createHmac('sha224', Buffer.from('secret key'))}
\DoxyCodeLine{hmac.update('synchronous write') //optional encoding parameter}
\DoxyCodeLine{hmac.digest() // synchronously get result with optional encoding parameter}
\DoxyCodeLine{}
\DoxyCodeLine{hmac.write('write to it as a stream')}
\DoxyCodeLine{hmac.end() //remember it's a stream}
\DoxyCodeLine{hmac.read() //only if you ended it as a stream though}

\end{DoxyCode}
 