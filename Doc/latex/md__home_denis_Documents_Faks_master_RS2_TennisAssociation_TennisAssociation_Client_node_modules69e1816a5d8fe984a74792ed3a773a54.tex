A node module to get your node module started.

\href{http://travis-ci.org/npm/init-package-json}{\texttt{ }}\hypertarget{md__home_denis_Documents_Faks_master_RS2_TennisAssociation_TennisAssociation_Client_node_modules69e1816a5d8fe984a74792ed3a773a54_autotoc_md14125}{}\doxysection{Usage}\label{md__home_denis_Documents_Faks_master_RS2_TennisAssociation_TennisAssociation_Client_node_modules69e1816a5d8fe984a74792ed3a773a54_autotoc_md14125}

\begin{DoxyCode}{0}
\DoxyCodeLine{var init = require('init-\/package-\/json')}
\DoxyCodeLine{var path = require('path')}
\DoxyCodeLine{}
\DoxyCodeLine{// a path to a promzard module.  In the event that this file is}
\DoxyCodeLine{// not found, one will be provided for you.}
\DoxyCodeLine{var initFile = path.resolve(process.env.HOME, '.npm-\/init')}
\DoxyCodeLine{}
\DoxyCodeLine{// the dir where we're doin stuff.}
\DoxyCodeLine{var dir = process.cwd()}
\DoxyCodeLine{}
\DoxyCodeLine{// extra stuff that gets put into the PromZard module's context.}
\DoxyCodeLine{// In npm, this is the resolved config object.  Exposed as 'config'}
\DoxyCodeLine{// Optional.}
\DoxyCodeLine{var configData = \{ some: 'extra stuff' \}}
\DoxyCodeLine{}
\DoxyCodeLine{// Any existing stuff from the package.json file is also exposed in the}
\DoxyCodeLine{// PromZard module as the `package` object.  There will also be three}
\DoxyCodeLine{// vars for:}
\DoxyCodeLine{// * `filename` path to the package.json file}
\DoxyCodeLine{// * `basename` the tip of the package dir}
\DoxyCodeLine{// * `dirname` the parent of the package dir}
\DoxyCodeLine{}
\DoxyCodeLine{init(dir, initFile, configData, function (er, data) \{}
\DoxyCodeLine{  // the data's already been written to \{dir\}/package.json}
\DoxyCodeLine{  // now you can do stuff with it}
\DoxyCodeLine{\})}

\end{DoxyCode}


Or from the command line\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\$ npm-\/init}

\end{DoxyCode}


See \href{https://github.com/npm/promzard}{\texttt{ Prom\+Zard}} for details about what can go in the config file. 